path: coyote/tasks.py
tasks:
    # down_iface:
        # needs_sudo: true
        # runs:
            # min: 5 min
            # max: 10 min
        # odds: 2%
        # args:
            # iface: eth0
            # timeout:
                # min: 30s
                # max: 10m
    cprint:
        runs:
            min: 2
            max: 5
        odds: 25%
        args:
            string: [test1, test2, test3]
